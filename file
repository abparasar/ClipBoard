# Create the arcface_model folder if not already present
!mkdir -p arcface_model

# Download backbone.pth directly to arcface_model
!wget https://github.com/neuralchen/SimSwap/releases/download/1.0/backbone.pth -P arcface_model/