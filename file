import pyarrow as pa
import pyarrow.ipc as ipc

# Open the file in binary mode
with open("your_file.arrow", "rb") as f:
    reader = ipc.RecordBatchFileReader(f)
    table = reader.read_all()

# Convert to pandas DataFrame if needed
df = table.to_pandas()

print(df.head())